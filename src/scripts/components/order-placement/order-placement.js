import { RightElementOfOrderPlacementView } from '@/scripts/components/order-placement/elements/right-element-of-order-placement/view/right-element-of-order-placement.ts';
import { orderData } from '@/scripts/components/order-placement/api/order-placement.ts';

/**
 * Функция для рендеринга шаблона размещения заказа и инициализации правого элемента.
 *
 * @returns {Promise<void>} Возвращает промис, который разрешается после того, как шаблон будет отрендерен и элемент инициализирован.
 */
/*export function orderPlacement() {
  return TemplateManager.templatize(
    document.getElementById('main'),
    '/src/scripts/components/order-placement/view/order-placement.hbs',
    orderData
  ).then(() => {
    new RightElementOfOrderPlacementView();
  });
}*/
