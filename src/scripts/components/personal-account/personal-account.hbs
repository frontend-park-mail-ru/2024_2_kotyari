<div class="account">
  <!-- Левая колонка -->
  <div class="column-personal-account">
    <div class="box-personal-account user-info">
      <div class="user-details">
        <img src="{{user.photoUrl}}" alt="User Photo" class="user-photo">
        <h2 class="user-name">{{user.name}}</h2>
        <span class="material-icons notification">{{#if user.notifications}}notifications_unread{{else}}notifications{{/if}}</span>
      </div>
      <div class="user-details">
        <div class="description-wrapper">
          <div class="description-item">
            <span class="description-key">Пол</span>: <span class="description-value gender">{{user.gender}}</span>
          </div>
          <div class="description-item">
            <span class="description-key">Почта</span>: <span class="description-value email">{{user.email}}</span>
          </div>
        </div>
        <button id="edit-user-info" class="edit-btn user-info" type="button">
          <span class="material-icons edit">edit</span>
        </button>
      </div>
      <button class="logout-button-personal-account" href="/logout" router="stability-active">Выход</button>

    </div>
  </div>

  <!-- Центральная колонка -->
  <div class="column-personal-account">
    {{#each deliveryInfo}}
      <div class="box-personal-account {{this.class}}">
        <div class="icon-personal-account">
          <span class="material-icons {{this.iconClass}}">{{this.icon}}</span>
        </div>
        <div class="{{this.detailsClass}}">
          <h3 class="{{this.titleClass}}">{{this.title}}</h3>
          <p class="{{this.textClass}}">{{this.text}}</p>
        </div>
        {{#if this.editable}}
          <button id="edit-user-address" class="edit-btn user-info" type="button">
            <span class="material-icons edit">edit</span>
          </button>
        {{/if}}
      </div>
    {{/each}}
  </div>

  <!-- Правая колонка -->
  <div class="column-personal-account">
    {{#each rightColumnInfo}}
      <div class="box-personal-account {{this.class}}">
        <div class="icon-personal-account">
          <span class="material-icons {{this.iconClass}}">{{this.icon}}</span>
        </div>
        <div class="{{this.detailsClass}}">
          <h3 class="{{this.titleClass}}">{{this.title}}</h3>
          <p>{{this.text}}</p>
        </div>
      </div>
    {{/each}}
  </div>
</div>
